export enum Environment {
  local = 'local',
  production = 'production',
  dev = 'dev',
  test = 'test',
}

export enum EQuerySort {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum LanguageCode {
  EN = 'en',
  VI = 'vi',
}
